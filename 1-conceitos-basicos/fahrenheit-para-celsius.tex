% !TeX encoding = UTF-8
% !TeX spellcheck = pt_BR
% !TeX root = ../prova.tex
% !Tex level = facil
% !Tex tags = 

Faça um algoritmo que leia uma temperatura em Fahrenheit e a apresente convertida em graus Celsius. A fórmula de conversão é $C = (F – 32) * ( 5 / 9)$, na qual F é a temperatura em Fahrenheit e C é a temperatura em Celsius.
